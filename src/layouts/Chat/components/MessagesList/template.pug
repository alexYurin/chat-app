-
  const classNames = {
    [`${className}`]: Boolean(className),
  }

  const [loader, ...items] = children

ul(class=classNames).messages-list.scroll
  | !{loader}
  each message in items
    li.messages-list__item
      | !{message}
