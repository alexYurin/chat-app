-
  const classNames = {
    [`${className}`]: Boolean(className),
    [`button_${status}`]: Boolean(status),
  }

button(
  id=id
  type=type
  name=name
  class=classNames
  value=value
  disabled=disabled
).button #{slot}
