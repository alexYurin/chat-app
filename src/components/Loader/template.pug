-
  const classNames = {
    [`${className}`]: Boolean(className),
    ['loader_visible']: isVisible,
  }

.loader(
  id=id
  class=classNames
)
  if (withOverlay)
    .loader__overlay

  .loader__content
    span.loader__item
    span.loader__item
    span.loader__item
